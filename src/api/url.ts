export const article = {
  all: '/api/articles',
  one: '/api/article',
  byId: '/api/article/:id'
}
export const schedule = {
  all: '/api/schedules',
  one: '/api/schedule',
  byId: '/api/schedule/:id'
}
export const user = {
  all: '/api/users',
  one: '/api/user',
  login: '/api/login',
  register: '/api/register',
  byId: '/api/user/:id'
}
export const resource = {
  all: '/api/resources',
  one: '/api/resource',
  byId: '/api/resource/:id'
}
export const project = {
  all: '/api/project',
  one: '/api/project',
  byId: '/api/project/:id'
}
export const message = {
  all: '/api/messages',
  one: '/api/message',
  byId: '/api/message/:id'
}
export const log = {
  all: '/api/logs',
  one: '/api/log',
  byId: '/api/log/:id'
}
export const dict = {
  all: '/api/dictionaries',
  one: '/api/dictionary',
  byId: '/api/dictionary/:id'
}